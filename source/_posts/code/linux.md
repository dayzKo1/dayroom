1，你用过哪些Linux命令？
文件与目录操作：ls，cd，mkdir,rm，cp，mv。
文件内容查看：cat，more/less，tail，head。
系统管理：ps，top/htop，kill/killall，df，du。
网络配置与调试：ping，ifconfig/ip，netstat/ss，curl/wget。
文件权限与用户管理：chmod，chown，useradd/userdel，passwd。
2
