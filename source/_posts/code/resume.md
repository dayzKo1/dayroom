---
title: 
date: 2025-3-10 12:00:00
---
1、gis、大屏
2、vue2、vue3、uni-app、react、wx小程序(支付)
3、

1.精通vue，react等SPA框架，有实际开发经验。
2.wx小程序，要求实际开发经验。
3.electron，tauri要求实际开发经验。
4.uni-app，要求实际开发经验。
5.ui框架诸如：element，echarts，antdesign，vant，vux等。
6.webpack，vite，Gulp，Grunt，等打包工具。
7.性能优化。
8.网络通信包括不限于axios，webSociket
9.三大马车（css，js/ts，html）更是基础中的基础
10.微前端：single-spa，qiankun，无界
11.有些还需要英语....
12.node.js服务端知识，express.js，NestJS，等等
12.了解sql数据库，docker，部署等等。
13.3d方向：three.js，webGl,Cocos等。
14.各种算法，数据结构，http协议等等。

如何保证用户的使用体验
如何解决页面请求接口大规模并发问题
大文件上传了解多少 
H5 如何解决移动端适配问题 
站点一键换肤的实现方式有哪些
如何实现网页加载进度条
常见图片懒加载方式有哪些
cookie 构成部分有哪些
扫码登录实现方式 
DNS 协议了解多少
函数式编程了解多少
前端水印了解多少
什么是领域模型
直在 window 上面挂东西是否有什么风险深度
SEO 优化的方式有哪些从技术层面来说小程序为什么会有两个线程


以下是基于你的简历梳理的 **10个技术岗高频面试问题及回答示例**，结合你的项目经历和技术栈进行针对性设计：

---

### **1. 你在“纪委福州市集体三资协同治理云图”项目中提到优化全局状态管理，具体如何通过mixins实现？遇到过哪些挑战？**  
**回答示例**：  
> "在该项目中，我们遇到地图组件与图表组件状态频繁交互的问题。通过mixins将地图缩放级别、时间筛选等公共状态抽离，例如创建`mapStateMixin`统一管理地图中心坐标和缩放参数，并在10+组件中复用。  
> **挑战**在于混入优先级冲突，我们通过规范命名空间（如`$_mapState`）和文档约束使用顺序解决。最终状态逻辑复用率提升40%，维护效率显著提高。"

---

### **2. 作为“采控网App”项目负责人，你是如何平衡React-Native与原生模块的性能问题的？**  
**回答示例**：  
> "在开发聊天室模块时，Rocket.Chat的频繁消息推送导致列表渲染卡顿。我们采取三阶段优化：  
> 1) **架构层**：使用Redux进行状态批处理，减少不必要的re-render；  
> 2) **组件层**：实现虚拟滚动（react-native-window-size）和图片懒加载；  
> 3) **原生层**：针对文件下载模块编写Java原生线程池管理。  
> 最终FPS从35稳定到60，安装包体积缩减18%。"

---

### **3. 你在多个项目中提到微前端实践（如qiankun），遇到过哪些沙箱隔离问题？如何解决的？**  
**回答示例**：  
> "在‘跨境电商SaaS建站后台’项目中，主子应用样式污染问题突出。我们通过：  
> 1) **CSS策略**：强制所有组件使用CSS Modules，并添加项目前缀（如`.biz-`）；  
> 2) **JS沙箱**：针对qiankun的Proxy隔离缺陷，对全局变量访问进行白名单控制；  
> 3) **构建优化**：为子应用配置独立的publicPath避免资源冲突。  
> 最终实现5个微应用并行运行零冲突，热更新速度提升25%。"

---

### **4. 你提到“使用Rust编写工具类实现多窗口路由”，为何选择Rust？与前端如何协同？**  
**回答示例**：  
> "在‘采控网桌面应用’中，需要处理系统级功能（如托盘菜单/多窗口通信）。选择Rust是因为：  
> 1) **性能优势**：内存安全特性适合高频IPC通信；  
> 2) **生态匹配**：Tauri框架天然支持Rust+前端组合；  
> 3) **跨平台**：一次编写即可生成Windows/macOS/Linux版本。  
> 我们通过`#[tauri::command]`注解暴露Rust接口，前端用TypeScript封装SDK，实现如窗口间状态同步等复杂功能。"

---

### **5. 在“政企AI服务平台”中接入DeepSeek流式输出，前端如何优化用户体验？**  
**回答示例**：  
> "针对流式对话场景，我们设计了三层优化：  
> 1) **交互层**：使用`EventSource`分块接收数据，配合`requestAnimationFrame`实现逐字渲染动画；  
> 2) **性能层**：对长对话采用增量DOM更新（snabbdom库），避免全量重绘；  
> 3) **容错层**：设置重试队列和本地缓存，在网络中断时自动续接。  
> 最终用户感知延迟降低60%，5000字以上对话流畅度提升显著。"

---

### **6. 你多次主导旧项目重构（如博思官网），请分享重构方法论**  
**回答示例**：  
> "重构遵循‘评估-试点-铺开’三步法：  
> 1) **量化评估**：用Lighthouse扫描性能指标，统计技术债文件（如耦合度>80%的组件）；  
> 2) **渐进式改造**：优先抽取通用服务层（如API请求/权限管理），再用微前端逐步替换模块；  
> 3) **保障机制**：配套Jest+Testing-Library实现核心链路85%覆盖率。  
> 例如在博思官网重构中，首屏加载时间从4.2s降至1.8s，CSS冗余减少73%。"

---

### **7. 作为组长如何推动代码规范落地？遇到过什么阻力？**  
**回答示例**：  
> "在福建采控期间，我主导制定了《前端工程化规范2.0》，推行策略包括：  
> 1) **工具先行**：配置ESLint+Prettier+husky强制校验，CI流水线拦截违规MR；  
> 2) **渐进实施**：旧项目设置`eslint-disable`豁免期，新项目严格执行；  
> 3) **正向激励**：月度评选‘规范之星’给予奖励。  
> 初期遇到老员工抵触，通过录制教学视频、组织结对编程逐步化解，3个月后团队代码CR通过率从65%提升至92%。"

---

### **8. 你提到“Jenkins/GitLab-CI自动化部署”，如何设计回滚机制？**  
**回答示例**：  
> "我们的CI/CD流程包含三级防御：  
> 1) **预发布阶段**：自动生成CHANGELOG并触发人工确认；  
> 2) **灰度发布**：通过K8s金丝雀部署，先对5%流量验证；  
> 3) **快速回滚**：构建时持久化产物到OSS，记录版本哈希。  
> 关键脚本逻辑示例：  
> ```bash
> # 失败时自动回滚至上一稳定版本
> ROLLBACK_TAG=$(git describe --abbrev=0 --tags)
> if [ $? -ne 0 ]; then
>   docker rollback ${PROJECT}-${ROLLBACK_TAG}
>   slack-alert "Rollback to $ROLLBACK_TAG"
> fi
> ```
> 该方案使平均故障恢复时间从30分钟缩短至2分钟。"

---

### **9. 你在性能优化中常提到“渲染/请求/内存”三个维度，请举例综合优化案例**  
**回答示例**：  
> "在‘福建省金融服务云平台’可视化大屏项目中：  
> **渲染优化**：对ECharts实例启用离屏Canvas，复用WebGL上下文；  
> **请求优化**：设计数据聚合网关，将20+API合并为1个GraphQL端点；  
> **内存优化**：实现大数据量的Web Worker代理计算，主线程内存占用降低65%。  
> 最终在6K+数据点实时渲染场景下，CPU占用率稳定在30%以下。"

---

### **10. 你同时掌握Vue/React，如何根据场景选择技术栈？**  
**回答示例**：  
> "我的技术选型框架包含四个评估维度：  
> 1) **团队因素**：现有技术储备（如福建码项目沿用Vue2因团队熟悉度）；  
> 2) **生态需求**：需要复杂状态管理时倾向React+Redux；  
> 3) **性能边界**：高频率更新场景选择Vue3+Proxy响应式；  
> 4) **长期演进**：新项目优先TypeScript+现代框架（如Vite+React18）。  
> 例如在‘政企AI服务平台’选择Vue2，因其模板语法更适配低代码配置；而‘采控网桌面应用’选React+TS因需要强类型保障复杂业务逻辑。"

---

### **附加准备建议**
1. **技术深挖**：针对简历中Rust/Tauri/GraphQL等差异化技术点，准备底层原理级回答（如"Tauri与Electron架构差异"）。
2. **数据敏感**：量化所有成果（如"性能提升X%"要能说明测试方法/基准数据）。
3. **架构思维**：对项目中的技术决策准备替代方案对比（如"为何选qiankun而非Module Federation"）。  
可结合STAR法则完善案例细节，重点突出**技术深度×业务价值**的交叉能力。


以下是基于你的简历补充的 **另外10个技术岗高频面试问题及回答示例**，进一步深挖技术细节与架构设计能力：

---

### **11. 在“政企AI服务平台”中封装本地化富文本组件，遇到哪些浏览器兼容性问题？如何解决？**  
**回答示例**：  
> "该组件需要支持IE11，但现代富文本库（如Tiptap）基于ProseMirror开发，不兼容旧浏览器。我们的解决方案：  
> 1) **分层架构**：核心使用`contenteditable`原生API实现基础编辑，高级功能（如协同编辑）动态加载Polyfill；  
> 2) **差异化构建**：通过`browserslist`区分生成ES5/ES6双版本；  
> 3) **降级策略**：检测到IE时自动禁用Markdown快捷键等非关键功能。  
> 最终在保证80%核心功能的前提下，IE11适配成本降低60%。"

---

### **12. 你多次使用Tauri开发桌面应用，与Electron相比有哪些性能优化点？**  
**回答示例**：  
> "在‘采控网桌面应用’技术选型时，我们对比发现：  
> 1) **内存占用**：Tauri应用平均内存使用为Electron的1/3（实测120MB vs 350MB）；  
> 2) **打包体积**：利用Rust编译优化，安装包从Electron的180MB缩减至28MB；  
> 3) **启动速度**：通过Rust预加载关键模块，冷启动时间从4.3s降至1.2s。  
> 但Tauri插件生态较弱，我们通过编写Rust FFI绑定C++库解决了打印机驱动集成问题。"

---

### **13. 你提到“使用GraphQL优化数据请求”，如何设计类型系统保证前后端协作效率？**  
**回答示例**：  
> "在‘跨境电商SaaS后台’项目中：  
> 1) **Schema契约**：使用GraphQL Codegen自动生成TypeScript类型定义和React Hooks；  
> 2) **版本控制**：通过`@deprecated`指令标记废弃字段，配合Sentinel监控字段使用率；  
> 3) **Mock机制**：基于Schema用MockServiceWorker生成测试数据。  
> 这使得前后端接口联调时间减少40%，类型错误率下降90%。"

---

### **14. 作为“福建码”前端主程，如何保证多环境（开发/测试/生产）API配置的灵活性？**  
**回答示例**：  
> "我们设计了三层配置体系：  
> 1) **环境变量**：使用`dotenv`管理基础API域名；  
> 2) **动态注入**：通过Webpack DefinePlugin在构建时注入版本号等元信息；  
> 3) **运行时覆盖**：在Docker容器启动时读取Consul配置中心的动态参数。  
> 关键代码示例：  
> ```js
> // config.js
> export const API_BASE = process.env.VUE_APP_API_BASE || window.__env.API_BASE;
> ```
> 该方案支持秒级环境切换，上线后零配置变更引发的事故。"

---

### **15. 在“纪委治理云图”中集成ArcGIS，如何优化大数据量地图渲染性能？**  
**回答示例**：  
> "面对10万+多边形地块数据：  
> 1) **数据分层**：按缩放级别动态加载LOD（Level of Detail）数据，缩放级别<12时显示简化拓扑；  
> 2) **WebGL加速**：将GeoJSON转换为Protobuf格式，通过Deck.gl渲染；  
> 3) **空间索引**：使用RBush建立R-tree索引，视口外要素自动卸载。  
> 最终在普通办公电脑上实现60FPS流畅渲染，内存峰值降低45%。"

---

### **16. 你主导过Jenkins脚本优化，如何实现多项目并行构建的资源分配？**  
**回答示例**：  
> "我们的Jenkins集群采用以下策略：  
> 1) **标签化节点**：根据项目类型（Web/App/Desktop）划分执行器，设置资源配额；  
> 2) **动态管道**：使用Jenkinsfile的`parallel`指令实现子模块并发构建；  
> 3) **缓存共享**：通过Nexus仓库复用node_modules，结合`checksum`跳过未变更任务。  
> 关键配置片段：  
> ```groovy
> stages {
>   stage('Build') {
>     parallel {
>       stage('Web') { agent { label 'web-builder' } ... }
>       stage('App') { agent { label 'mobile-builder' } ... }
>     }
>   }
> }
> ```
> 整体构建时间从25分钟缩短至8分钟。"

---

### **17. 在“采控网App”中接入阿里云消息推送，如何保证离线消息的可靠投递？**  
**回答示例**：  
> "我们设计了消息可靠性的‘三级缓冲’机制：  
> 1) **客户端队列**：使用RxJS管理待发送消息，网络恢复后自动重试；  
> 2) **服务端去重**：为每条消息生成UUID，服务端用Redis SETNX去重；  
> 3) **最终一致性**：每天凌晨启动补偿任务，扫描未确认消息。  
> 结合消息状态机（待发送/已发送/已送达/已读），实现99.99%的消息可达率。"

---

### **18. 你提到“使用qiankun嵌入Jira页”，如何处理跨域身份验证？**  
**回答示例**：  
> "该场景需要将Jira的OAuth2.0鉴权嵌入到Tauri桌面应用：  
> 1) **主应用代理**：通过Tauri的Rust后端创建反向代理，添加CORS头；  
> 2) **Token同步**：使用BroadcastChannel在主-子应用间同步JWT；  
> 3) **安全加固**：设置HttpOnly Cookie存储Token，每次请求携带双重签名。  
> 最终实现无缝登录态共享，XSS攻击面减少70%。"

---

### **19. 在“官渡企业帮扶小程序”中优化文件上传，具体方案是什么？**  
**回答示例**：  
> "针对大文件上传痛点：  
> 1) **分片上传**：将文件切割为2MB chunks，用Web Worker并行上传；  
> 2) **断点续传**：基于File API的slice方法记录已传分片索引；  
> 3) **预览优化**：对PDF文件使用Mozilla的pdf.js实现前端预览。  
> 测试显示100MB文件上传成功率从65%提升至99.5%，流量节省30%。"

---

### **20. 你多次实施微前端改造，如何设计跨应用状态共享方案？**  
**回答示例**：  
> "在‘跨境电商SaaS后台’的微前端架构中：  
> 1) **轻量级总线**：使用CustomEvent实现跨应用事件通信；  
> 2) **共享存储**：将用户权限等公共状态存放在主应用的Redux中；  
> 3) **持久化策略**：通过localStorage同步语言偏好等低频数据。  
> 关键代码示例：  
> ```js
> // 主应用注册全局store
> window.sharedStore = createSharedStore();
> 
> // 子应用监听
> window.addEventListener('store-update', (e) => {
>   updateLocalState(e.detail);
> });
> ```
> 该方案使跨应用状态同步延迟小于50ms。"

---

### **应对策略补充**
1. **技术趋势**：准备对前沿技术的看法（如“Rust在前端工具链的应用前景”）。
2. **故障复盘**：针对简历项目，预设一个线上事故案例及解决过程（可用5WHY分析法描述）。
3. **架构图准备**：对复杂项目（如纪委云图）绘制简易架构图，面试时可用白板演示。  
始终强调 **技术决策与业务目标的关联性**（如“选择Tauri不仅因性能，更因客户现场的国产化系统兼容需求”）。



Why Not Iframe
为什么不用 iframe，这几乎是所有微前端方案第一个会被 challenge 的问题。但是大部分微前端方案又不约而同放弃了 iframe 方案，自然是有原因的，并不是为了 "炫技" 或者刻意追求 "特立独行"。
如果不考虑体验问题，iframe 几乎是最完美的微前端解决方案了。
iframe 最大的特性就是提供了浏览器原生的硬隔离方案，不论是样式隔离、js 隔离这类问题统统都能被完美解决。但他的最大问题也在于他的隔离性无法被突破，导致应用间上下文无法被共享，随之带来的开发体验、产品体验的问题。
url 不同步。浏览器刷新 iframe url 状态丢失、后退前进按钮无法使用。
UI 不同步，DOM 结构不共享。想象一下屏幕右下角 1/4 的 iframe 里来一个带遮罩层的弹框，同时我们要求这个弹框要浏览器居中显示，还要浏览器 resize 时自动居中..
全局上下文完全隔离，内存变量不共享。iframe 内外系统的通信、数据同步等需求，主应用的 cookie 要透传到根域名都不同的子应用中实现免登效果。
慢。每次子应用进入都是一次浏览器上下文重建、资源重新加载的过程。
其中有的问题比较好解决(问题1)，有的问题我们可以睁一只眼闭一只眼(问题4)，但有的问题我们则很难解决(问题3)甚至无法解决(问题2)，而这些无法解决的问题恰恰又会给产品带来非常严重的体验问题， 最终导致我们舍弃了 iframe 方案。